# Python cache
__pycache__/
*.pyc
*.pyo

# Log files
calibration_*.log
pipeline_*.log
future_multi_*.log

# Raven error files
Raven_errors.txt

# Literature PDFs — large binaries, kept local only (rsync to server if needed)
paper_ideas/multiobj_pdfs/
paper_ideas/*_pdfs/

# Generated plots — large binaries, regenerable from data; keep out of git
plots/

# Local download caches (raw HDFs, etc. — regeneratable from source)
downloads/cache/

# Generated validation figures — regeneratable via scripts/validate_glacier_coverage.py
outputs/glacier_validation/
