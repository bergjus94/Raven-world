# SMOKE TEST: Structure S5 on catchment 2256 — SPHY-faithful (direct overland + cascade).
# Expected calibrated param count: 14 (S1's 12 minus X11 plus X12+X13+X14).
# Validates: no :Flush in .rvi; cascade percolation TOPSOIL→FAST→SLOW in .rvi;
#            FAST_RES still drains via BASE_LINEAR; no BASE_THRESH_STOR; no :Split.

catchment: '2256'
display_name: 'Rosegbach @ Pontresina'
region: 'switzerland'

start_date: '2000-01-01'
end_date: '2020-12-31'
cali_end_date: '2010-12-31'
warm_up_date: '1990-01-01'

warmup:
  method: 'real'

precip_correction: true

models:
  - SPHY

configurations:
  - glogem_subdaily_opt2_sphy_faithful

calibration:
  metrics: ['S5_smoke']
  iterations: 10
  ngs: 3

  objectives: [Q]
  Q:
    metric: KGE

future:
  enabled: false
